<?php

/**
 *  Operador Ternário:
 *  Explicação: É como se fosse o if e else mais enxuto
 *     1 PASSO: Declaramos a condição;
 *     2 PASSO: logo após o sinal de interrogação (?);
 *     3 PASSO: o código a ser executado caso, a condição seja verdadeira; e depois dos dois pontos,o código a ser
 * executado caso contrário.
 */

//Exemplo 1:

/**$idade = 19;

$mensagem = $idade < 18 ? 'Você é menor de idade' : 'Você é maior de idade';
echo $mensagem; **/

// Exemplo 2:

$a = 8;
$b = 2;

echo $a/$b == 2 ? "O resultado da divisão é 2" : "O resultado da divisão não é 2";

?>

